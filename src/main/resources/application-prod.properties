spring.datasource.driverClassName=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false
spring.h2.console.enabled=false
app.jwt.secret=${JWT_SECRET:ambulatoFemaSistemaSeguroT16PediatriaMedicina2024SuperSecretKey}
app.jwt.expiration=86400000
app.cors.allowed-origins=${CORS_ORIGINS:https://pediatrianm.vercel.app}
server.port=${PORT:8080}
```

Agora o Spring Boot vai pegar `SPRING_DATASOURCE_URL`, `SPRING_DATASOURCE_USERNAME` e `SPRING_DATASOURCE_PASSWORD` automaticamente das variáveis de ambiente que o Railway injeta.

**Passo 3 — Commit e push:**
```
git add .
git commit -m "Fix database connection Railway"
git push